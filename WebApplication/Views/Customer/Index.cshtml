
@{
    ViewBag.Title = "Müşteri Listesi";
    Layout = "~/Views/Shared/_LayoutPage.cshtml";
}

@model List<WebApplication.Models.CustomerDTO>
<div class="card">
    <div class="card-body">
        <div class="card-title">
            <div class="row col-12">
                <div class="col-6" id="baslik">
                    Müşteri Listesi
                </div>
                <div class="col-6">
                    <a href="/Customer/CustomerAdd" class="btn btn-sm btn-success float-right font-weight-bold"><i class="la la-plus"></i> Müşteri Ekle</a>
                </div>
            </div>
        </div>
        <table class="table table-striped text-center">
            <thead>
                <tr>
                    <th>ID</th>
                    <th>Firma Adı</th>
                    <th>Telefon</th>
                    <th>Email</th>
                    <th>Durum</th>
                    <th>İşlemler</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@item.ID</td>
                        <td>@item.CustomerName</td>
                        <td>@item.Phone</td>
                        <td>@item.Email</td>
                        <td>
                            @if (item.IsActive == true)
                            {
                                <a href="#" onclick="customerStatusUp(@item.ID)"><span class="badge badge-success badge-pill">Aktif</span></a>
                            }
                            else
                            {
                                <a href="#" onclick="customerStatusUp(@item.ID)"> <span class="badge badge-danger badge-pill">Pasif</span></a>
                            }
                        </td>
                        <td>
                            <a href="/Customer/CustomerUpdate/@item.ID" class="btn btn-sm btn-warning"><i class="la la-edit"></i> Güncelle</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
@section JS{
    <script src="~/Assets/Custom/CustomerJS.js"></script>
}
